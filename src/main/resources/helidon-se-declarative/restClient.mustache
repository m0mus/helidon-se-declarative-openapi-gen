package {{package}};

// TODO: verify the exact import for @RestClient.Endpoint in your Helidon version.
// In Helidon 4.4.x the annotation lives in io.helidon.webclient.api or a sub-package.
// import io.helidon.webclient.api.RestClient;

{{#operations}}
/**
 * Declarative REST client for the {{classname}} API.
 *
 * <p>Extends {@link {{classname}}Api} to inherit all HTTP binding annotations.
 * The Helidon annotation processor generates a proxy implementation at build time.</p>
 *
 * <p>Configure the target URI via application.yaml:
 * <pre>
 *   {{classnameLowercase}}-service:
 *     uri: http://localhost:8080
 * </pre>
 * </p>
 *
 * <p>Add to pom.xml:
 * <pre>{@code
 *   <dependency>
 *     <groupId>io.helidon.webclient</groupId>
 *     <artifactId>helidon-webclient-http1</artifactId>
 *   </dependency>
 * }</pre>
 * </p>
 */
// Uncomment after adding helidon-webclient-http1 dependency and verifying import path:
// @RestClient.Endpoint("${" + "{{classnameLowercase}}-service.uri:http://localhost:8080}")
public interface {{classname}}Client extends {{classname}}Api {
    // No additional methods — inherits the full API contract from {{classname}}Api
}
{{/operations}}
